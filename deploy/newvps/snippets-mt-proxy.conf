# Install to: /etc/nginx/snippets/mt-proxy.conf
# Shared reverse-proxy settings -> Kestrel loopback. Included by the managertool site.
proxy_pass http://127.0.0.1:5281;
proxy_http_version 1.1;
proxy_set_header Host              $host;
proxy_set_header X-Real-IP         $remote_addr;
proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
proxy_set_header X-Forwarded-Proto $scheme;
# WebSocket upgrade (SignalR /hub). $connection_upgrade comes from the map in the site file.
proxy_set_header Upgrade           $http_upgrade;
proxy_set_header Connection        $connection_upgrade;
proxy_read_timeout 3600s;
proxy_send_timeout 3600s;
